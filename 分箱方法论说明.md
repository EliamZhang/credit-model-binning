# 模型分数分箱与策略阈值设定方法说明

## 一、总体思路

模型输出连续风险分（score_mlt），业务侧需要将其转化为稳定、单调、可解释的风险等级，并据此确定自动通过、人工审核、拒绝三段阈值。本文档说明当前分箱与阈值方案的方法论与验证逻辑，供评审确认。

整体流程：在 Development 上等频初分 → 约束自动合箱为 6~8 档（目标 7 档）→ 候选方案评分择优 → 映射全部切片 → 风险约束下确定阈值 → OOT 独立验证 → 输出 Excel 策略报告。

> 样本切分（Train / OOT）→ Train 内再切分（Development / Validation）
> → Development 上 20 等频初分 → 边界复用到全样本
> → 自动合箱（小箱清理 → 单调合并 → 档位压缩 → 候选生成）
> → 候选方案评分与选择 → 映射到全部切片
> → 验证（单调性 / PSI / AUC-KS / 月度稳定性）
> → 阈值曲线与三段策略 → 输出报告

**图 1 整体技术路线**

方法论遵循三条设计原则：

1. **口径隔离**：分箱边界与合箱决策只在 Development（必要时参考 Validation）上学习，OOT 全程不接触，保证验证结果的独立性。
2. **可追溯**：合箱不是一步到位的黑箱，而是按"小箱清理 → 单调合并 → 档位压缩 → 候选生成"四个阶段逐步执行，每一步记录合并前后的范围、档位数、风险差异、显著性检验 p 值、IV 损失与合并原因，全流程可复核。
3. **可解释优先**：分箱结果必须满足单调性与单箱统计稳健性硬约束，合箱/评分只是在这些约束下择优，不牺牲可解释性换取形式上的指标最优。

## 二、样本设计与风险指标

### （一）样本切分

**表 1 样本切分设计**

| 切片              | 时间范围                          | 职责                                      |
| ----------------- | --------------------------------- | ----------------------------------------- |
| Development       | Train 内除最后 3 个月外的全部月份 | 学习分箱边界、执行合箱、检查约束与单调性  |
| Validation        | Train 内最后 3 个月               | 评估跨期稳定性（PSI、倒挂），参与方案择优 |
| OOT（时间外验证） | Train 截止月份之后                | 独立验证，不参与任何合箱与阈值调参        |

- 分箱边界仅在 Development 上学习，原样复用到 Validation、完整 Train 与 OOT，保证四切片口径一致。
- Validation 模拟近期分布漂移，用于判断方案稳健性；OOT 全程不接触，作为最终"考卷"。

**切分执行规则（与代码实现对齐）**：

- 按申请月份（application_month，格式须为可按字符串比较的 YYYY-MM）切分：`application_month ≤ TRAIN_END_MONTH` 进入 Train，`≥ OOT_START_MONTH` 进入 OOT；月份缺失或不在上述范围的样本不进入任何切片，不参与分析。
- Train 内切分 Development / Validation 时，优先取最后 `VALIDATION_MONTH_COUNT`（默认 3）个完整月份作 Validation，其余为 Development，且 Development 至少保留 3 个月；若 Train 总月份数不足 4 个月，则按申请时间顺序切出最后 20% 作 Validation（兜底规则 `FALLBACK_DEVELOPMENT_SAMPLE_PCT = 0.80`）。
- 分箱分析只使用存在模型分的样本；模型分缺失样本不进任何分箱统计，缺失量与缺失率在报告 `01_总览` 单独展示。

### （二）风险指标

**表 2 风险指标体系**

| 维度     | 指标       | 定义                         | 作用                 |
| -------- | ---------- | ---------------------------- | -------------------- |
| 观察窗口 | 1M30+      | 到期 1 个月内逾期 30 天以上  | 短期风险，反应快     |
| 观察窗口 | 3M30+      | 到期 3 个月内逾期 30 天以上  | 中期风险，更成熟稳定 |
| 计量口径 | 笔数逾期率 | 逾期样本数 ÷ 成熟样本数     | 人群风险暴露面       |
| 计量口径 | 金额逾期率 | 逾期剩余本金 ÷ 成熟本金敞口 | 金额损失强度         |

- 成熟样本定义：表现标签已确定（0/1）的样本才进入逾期率分母，未成熟样本不参与。
- 合箱主指标：1M30+ 与 3M30+ 笔数逾期率双指标，任一出现相邻倒挂即触发合并；显著性检验锚定 3M30+（`PRIMARY_RATE_COL`）。金额口径用于候选评价与策略验证。

**四类指标口径的精确公式**：

```text
笔数口径（1M30+ / 3M30+ 分别计算）：
  成熟样本  mature  = duedate ∈ {0, 1} 的样本数
  坏样本    bad     = duedate = 1 的样本数
  好样本    good    = mature − bad
  笔数逾期率        = bad / mature

金额口径（1M30+ / 3M30+ 分别计算）：
  成熟条件  = dpd_days_ever_mob1 / mob3 非空
  金额敞口  exposure = 所有成熟样本的 principal 之和
  逾期金额  bad_amt = dpd_days_ever ≥ 30 样本的 estimate_principal_remaining_mob1 / mob3 之和
  金额逾期率         = bad_amt / exposure
```

**派生指标**：

```text
样本占比  sample_pct = 箱内样本数 / 全部箱样本数
Lift      = 箱内逾期率 / 样本组整体逾期率（Lift > 1 表示风险高于整体）
累计指标  = 按低风险向高风险逐箱累加（cum_n、cum_pass_rate、cum_*_bad_rate 等）
```

置信区间：所有笔数逾期率点估计（单箱、累计、边际口径）均输出 **95% Wilson 置信区间**：

```text
p̂ = bad / mature，z = 1.96
下/上界 = (p̂ + z²/2m ± z·√(p̂(1−p̂)/m + z²/4m²)) / (1 + z²/m)　(1)
```

其中 m 为成熟样本量。小样本尾部箱应结合区间上界（保守风险上界）解读风险率，不能只看点估计。

## 三、分箱方法论

### （一）初分与档位目标

在 Development 上按分数分位数做 20 等频初分（`pd.qcut(..., q=20, duplicates="drop")`），区间规则 (left, right]，首尾边界扩展为 ±∞；分数重复过多时相同边界被合并去重，实际箱数可能少于 20，若不足最小最终档位数 6 则直接报错。初始箱编号为 B01、B02……B20，按分数从低到高排列（当前模型为高分高风险）。

Development 学到的边界是后续一切分析的唯一基准：

- 同一组边界用 `pd.cut` 原样应用到 Validation、完整 Train、OOT 与全量数据，保证四个切片的箱口径完全一致；
- 初始箱指标在 Development 与 Validation 上分别计算（无样本的箱也保留，防止候选范围错位）；
- 后续合箱决策只基于这两组初始箱统计，OOT 不参与。

### （二）单箱硬约束

**表 3 最终档位单箱硬约束（Development 上检查）**

| 约束项       | 普通箱要求                   | 说明                               |
| ------------ | ---------------------------- | ---------------------------------- |
| 样本占比     | 中间箱 ≥ 5%，头尾箱 ≥ 2.5% | 尾部箱允许更小，保证极端风险可分档 |
| 主指标成熟量 | ≥ 1,000                     | 保证逾期率估计的统计稳健性         |
| 坏样本量     | ≥ 20                        | 保证风险估计不是由个别样本驱动     |
| 好样本量     | ≥ 200                       | 保证低风险估计的稳定性             |

同时引入极端人群圈选保护：默认把最好（低风险端）与最坏（高风险端）各 1 个初始箱圈定为极端箱，不允许常规合箱跨越其边界，并放宽约束（成熟量 ≥ 500，最好箱坏样本下限 0、最坏箱好样本下限 0）。最好 / 最坏人群分别对应自动通过与拒绝策略的关键结构，不能被普通合箱稀释。

**检查口径**：

- 四项约束同时满足才算该箱合格（`all_constraints_ok`）；任一违反即触发小箱清理阶段。
- 违反严重度（`violation_severity`）按相对缺口累加：各维度 `max(0, 1 − 实际值/要求值)` 之和，用于决定小箱清理阶段优先处理哪个箱——缺口越大的箱越先被合并。

各切片分箱统计（`03_最终分箱统计`）与阈值曲线（`04_策略方案 · 阈值选择过程`）均随点估计输出 95% Wilson 置信区间（下界 / 上界，含累计与边际口径），小样本尾部箱应结合区间上界（保守风险上界）解读风险率，不能只看点估计。

### （三）单调性

- Development：主指标（1M30+、3M30+ 笔数逾期率）相邻箱不允许倒挂（容差 0，即严格非递减）。
- Validation：允许不超过 0.3 个百分点的容忍倒挂（模拟跨期小幅波动）。
- 候选评分同时监控 Validation 上四类风险率（含金额口径）的倒挂数。

**检查口径**：对每个指标按箱序计算相邻差 `diff = rate[i] − rate[i−1]`，`diff < −容差` 即计一次倒挂；双主指标中任一出现倒挂都会计数。单调性是风险等级可解释、可监控的基础，也是业务沟通与合规评审的基本要求。

### （四）保护边界

以下关键边界在自动合箱中尽量保留。边界编号 k 表示第 k 与 k+1 个初始箱之间的切点。识别规则：

- **策略风险边界**：对自动通过与总接纳两套约束分别处理——①累计 3M30+ 笔数逾期率首次越过对应上限的位置（取满足 `cum_3m30p_cnt_bad_rate ≤ 上限` 的最后一个箱的右边界）；②边际 3M30+ 笔数逾期率首次超过对应上限的位置（取首个超限箱的左边界）。
- **风险跳升边界**：相邻箱之间 3M30+ 风险跳升最大的边界，默认保护 1 个（`PROTECT_LARGEST_RISK_JUMPS = 1`）。
- **极端人群圈选边界**：最好 / 最坏极端箱的外侧边界（默认硬保护，禁止跨越）。

两类保护机制的强度不同：

| 机制           | 代价/惩罚                    | 可否跨越                                   |
| -------------- | ---------------------------- | ------------------------------------------ |
| 策略保护边界   | 计入合并代价 +100            | 小箱清理与单调修复阶段可跨越（强制修病优先） |
| 极端圈选边界   | 硬禁止（默认），或 +10,000 代价 + 评分 −50/次 | 默认不可跨越；配置开关放开后仍属高代价操作 |

策略阈值最终落在箱边界上，保护边界防止合箱吞掉策略关键结构；同时允许"先修病"（清理小箱、修复倒挂）时跨越策略保护边界，避免硬保护导致无可行方案。

### （五）合箱流程

**表 4 合箱四阶段**

| 阶段                     | 触发条件               | 合并策略                                                                           |
| ------------------------ | ---------------------- | ---------------------------------------------------------------------------------- |
| ① 小箱清理              | 存在违反单箱硬约束的箱 | 优先处理违反最严重的箱，仅允许与其相邻箱合并                                       |
| ② 单调合并（PAVA 风格） | 主指标存在相邻倒挂     | 从倒挂最严重的一对开始合并，直至无倒挂                                             |
| ③ 档位压缩              | 档位数仍多于 8 档      | 反复合并合并代价最低的相邻对                                                       |
| ④ 候选生成              | 档位数仍多于 6 档      | 优先合并"风险差异不显著（p ≥ 0.10）或差距 ≤ 0.3%"的相邻对，生成 8、7、6 档候选 |

每个阶段记录合并原因与前后状态，全过程可追溯；四阶段体现"先修病、再修型、后择优"的思路。

**各阶段的精确算法**：

```text
① 小箱清理（循环直至无违规箱或档位数到下限）：
   1) 计算当前各箱约束明细，取 violation_severity 最高的违规箱；
   2) 只允许该箱与其左/右相邻箱合并，二选一中取合并代价最低者；
   3) 允许跨越策略保护边界（修病优先），但极端圈选边界始终禁止跨越；
   4) 合并后回到 1)。

② 单调合并（循环直至无倒挂或档位数到下限）：
   1) 找出所有出现主指标倒挂的相邻对（双主指标任一倒挂即入选）；
   2) 从"跌幅最严重"的一对开始（双主指标下取两者中跌幅更大者），一次只合并一对；
   3) 允许跨越策略保护边界，极端圈选边界始终禁止；
   4) 合并后回到 1)。

③ 档位压缩（循环直至档位数 ≤ 8）：
   从全部相邻对中选合并代价最低者合并；保护边界按正常代价计费，极端边界禁止跨越。

④ 候选生成（循环直至档位数 ≤ 6）：
   1) 候选对 = 未跨越极端边界的所有相邻对；
   2) 若存在"两比例检验 p ≥ 0.10 或 |风险差距| ≤ 0.3%"的相邻对，则从这些对中取合并代价最低者合并（优先保留风险有区分度的边界）；
   3) 若无此类相邻对，则从全部候选对中取合并代价最低者合并（纯档位精简）；
   4) 每次合并后生成一个候选方案，记录合并原因。
```

**候选方案集合**：初始 20 箱状态与每一步合箱后的状态都是候选方案（按合箱范围去重），因此候选方案覆盖"原始初分 → 各阶段中间态 → 8/7/6 档精简态"，最终由评分环节从全部可行候选中选择，而非只比较阶段末状态。

### （六）合并代价

```text
merge_cost = 风险率差距 × 100 + (1 − 两比例Z检验p值) + IV损失 × 10 + 保护边界惩罚　(2)
保护边界惩罚：普通保护边界 100；极端圈选边界 10,000　(3)
```

各项来源与口径：

- **风险率差距**：取 1M30+、3M30+ 两主指标中更大的绝对差距（任一指标差异显著时都不会被轻易合并）；
- **两比例 Z 检验 p 值**：锚定 3M30+，用合并池化方差的双侧 Z 检验计算两箱逾期率差异的显著性（不依赖统计库，可直接复算）；
- **IV 损失**：`max(0, 合并前 IV − 合并后 IV)`，用箱级好坏样本量计算（含平滑项 0.5，避免零占比箱导致无穷），衡量合并对区分度的破坏；
- **保护边界惩罚**：跨越策略保护边界 +100，跨越极端圈选边界 +10,000（仅在配置允许极端边界跨越时才会实际出现）。

风险差距越大、差异越显著、IV 损失越大、跨越保护边界，代价越高，越不应合并。

### （七）候选评分与选择

四阶段产生的每一步合箱状态均为候选方案，先做硬约束判定，再对可行方案打分择优：

- **硬约束（必须全部满足）**：档位数 6~8；Development 主指标无倒挂；单箱约束全部满足；未跨越极端圈选边界。

**表 5 候选方案评分权重**

| 评分项                  | 权重                         | 含义                 |
| ----------------------- | ---------------------------- | -------------------- |
| 硬约束全部满足          | +100                         | 可行方案的基础分     |
| Development 主指标倒挂  | −30 × 次数                 | 惩罚开发期单调性缺陷 |
| Validation 主指标倒挂   | −12 × 次数                 | 惩罚跨期单调性缺陷   |
| Validation 全指标倒挂   | −4 × 次数                  | 惩罚金额口径倒挂     |
| 单箱约束违反            | −15 × 次数                 | 惩罚小箱残留         |
| Validation PSI 超偏好线 | −150 × max(0, PSI − 0.05) | 惩罚跨期分布漂移     |
| IV 保留率               | +12 × min(保留率, 1.5)      | 奖励区分度保留       |
| 最小相邻风险差距        | +100 × max(0, 最小差距)     | 奖励相邻档风险分离度 |
| 档位距离                | −1.5 ×\|档位数 − 7\|      | 偏好目标档位数       |
| 极端边界违反            | −50 × 次数                | 惩罚跨越极端圈选边界（可行方案恒为 0，仅无可行方案兜底时生效） |

**评分指标口径**：

- Validation PSI：以 Development 各档样本占比为基准、Validation 为对比计算（平滑项 1e−6），衡量同箱结构下的跨期分布漂移；
- IV 保留率：`合并后主指标 IV ÷ 初始 20 箱 IV`，超过 1.5 封顶；
- 最小相邻风险差距：取 1M30+、3M30+ 两主指标相邻增量中更小者，为负表示存在倒挂（此时该项为 0），保证任一指标区分度不足都会被识别；
- 权重为经验值，只影响候选排序，不影响硬约束判定。

**选择顺序**（依次排序，取第一名）：

```text
① 硬约束通过（1 优先 / 0 靠后）
② Development 主指标倒挂数（少优先）
③ 单箱约束违反数（少优先）
④ Validation 主指标倒挂数（少优先）
⑤ Validation PSI ≤ 0.10（可接受）
⑥ Validation PSI ≤ 0.05（偏好）
⑦ 综合得分（高优先）
⑧ IV 保留率（高优先）
⑨ 档位距离（|档位数 − 7| 小优先）
```

**兜底逻辑**：若没有任何候选通过全部硬约束，忽略硬约束筛选，直接从全部候选中按同一顺序取第一名；此时 `hard_constraints_ok = False`，需在 `02_分箱详情` 的候选评分表中确认该方案为何退化以及其可解释性。

## 四、策略阈值设定

在完整 Train 上，沿低风险到高风险方向逐档放宽阈值，构造累计与边际风险曲线：

- **候选阈值** = 最终箱右边界原始值（末档右边界为 +∞，用 Train 最大分数替代，保留"全量通过"点）；
- **累计口径**：分数 ≤ 当前阈值的全部人群（`cum_pass_rate`、累计笔数/金额逾期率及 Wilson 区间）；
- **边际口径**：相对上一个阈值新增进入的人群（`marginal_*_bad_rate`），用于识别风险拐点——累计风险尚可但边际风险快速上升，说明阈值已接近风险拐点。

**表 6 策略风险约束（示例配置）**

| 约束阶段              | 累计 1M30+ 笔数逾期率 | 累计 3M30+ 笔数逾期率 | 边际 3M30+ 笔数逾期率 |
| --------------------- | --------------------- | --------------------- | --------------------- |
| 自动通过              | ≤ 0.90%              | ≤ 5.50%              | ≤ 9.00%              |
| 总接纳（自动 + 人工） | ≤ 1.30%              | ≤ 7.50%              | ≤ 17.00%             |

**阈值选择规则**：

1. **自动通过阈值**：在阈值曲线中过滤掉任一自动通过约束不满足的行，余下候选中取累计通过率最高者（并列时取档位更靠后者）；
2. **总接纳阈值**：对总接纳约束做同样的过滤与选择；
3. **对齐规则**：总接纳阈值不得严于自动通过阈值，若出现（总接纳档位序 < 自动通过档位序）则把总接纳阈值对齐到自动通过阈值。

满足约束且累计通过率最高的阈值当选，形成三段策略：

```text
自动通过：score ≤ 自动通过阈值　(4)
人工审核：自动通过阈值 < score ≤ 总接纳阈值　(5)
拒绝：score > 总接纳阈值　(6)
```

约束给出风险上限，目标为通过率最大化，实现"风险可控前提下业务量最大"的平衡。

> 报告中 `04_策略方案 · 阈值敏感性` 对自动通过 / 总接纳阈值各输出当前、收严一档、放松一档的通过率、风险率与人工审核量对比（一档 = 相邻箱边界），供风险与业务确认表 6 上限取值时参考。

### 阈值上线执行规则

1. **分数精度与边界精度**：线上评分引擎输出与离线一致的 float 模型分（score_mlt），不限制小数位；阈值 = 最终箱右边界原始值（末档为 Train 最大分数），不做二次取整。
2. **阈值取整**：默认按原始精度部署；若工程必须取整（如存储小数位限制），只允许向更严方向取整——自动通过阈值、总接纳阈值均向下取整（floor），不放大接纳人群；取整后须重新计算三段占比与风险，并与未取整版本对比确认无风险放大。
3. **区间开闭**：采用 `(left, right]`——自动通过 `score ≤ 自动通过阈值`、人工审核 `自动通过阈值 < score ≤ 总接纳阈值`、拒绝 `score > 总接纳阈值`；分数精确等于阈值时归入右闭档（score == 阈值 → 通过/接纳）；线上用数值比较，禁止格式化字符串比较，避免边界落入真实分数值时产生错档。
4. **空值与异常值**：线上无法产出模型分或为空 → 按拒绝处理；NaN / Inf 不入自动通过，按拒绝处理；分数超出训练范围 → 归入对应极端箱（±∞ 边界兜底）。注意离线报告口径为"缺失分样本从分箱统计剔除，缺失量与缺失率见 01_总览"，与线上"缺失即拒"不同，两个口径需知悉。
5. **离线 / 线上一致性校验**：上线前逐项核对阈值清单（阈值原始值、开闭符号、末档全量通过点）；取最近批次样本做离线分档 vs 线上引擎分档对照，一致率必须 100%，重点覆盖等于边界及边界 ± 1 ulp 的分数；上线后监控线上分档占比与离线报告各档占比（PSI），边界漂移及时告警复核。

> 报告 `06_附录 · 上线执行规则` 输出上述规则的静态清单，供引擎团队上线时逐项核对。

## 五、方案验证

**表 7 验证指标体系**

| 验证维度 | 指标       | 口径 / 判定                                                                                  |
| -------- | ---------- | -------------------------------------------------------------------------------------------- |
| 风险排序 | 单调性     | 四切片（Dev / Val / Train / OOT）× 四类风险率是否非递减，输出倒挂次数与位置                 |
| 分布稳定 | PSI        | Train 与 OOT 各档样本占比差异；< 0.10 稳定，0.10~0.25 关注，≥ 0.25 明显变化                 |
| 区分能力 | AUC / KS   | Train / OOT × 1M30+ / 3M30+ 分别计算                                                        |
| 时间稳定 | 月度稳定性 | 逐月检查主指标单调性、倒挂次数、最大单次风险跌幅，识别异常月份                               |
| 策略效果 | 分段验证   | 自动通过 / 人工审核 / 拒绝三段在 Train 与 OOT 的规模与风险，应呈"低 < 中 < 高"梯度且不反转 |

验证贯穿三个层次：Development 检查方案自身质量，Validation 检查跨期稳健性，OOT 作为独立数据做最终确认。

**各指标的计算口径**：

```text
单调性：按箱序计算相邻差 diff = rate[i] − rate[i−1]，diff < 0 计一次倒挂；
       四切片 × 四类风险率（1M30+、3M30+ 笔数与金额）分别检查，输出倒挂次数与位置。

PSI：   Σ (实际占比 − 基准占比) × ln(实际占比 / 基准占比)，分母加 1e−6 平滑；
       基准 = Train 各档占比，实际 = OOT 各档占比。
       经验判定：< 0.10 稳定；0.10 ~ 0.25 关注；≥ 0.25 明显变化。
       合箱选型另有更严的两档阈值：偏好 ≤ 0.05（Validation 对 Development），可接受 ≤ 0.10。

AUC：   秩和方法（Mann-Whitney U 统计量等价式），
       AUC = (坏样本秩和 − 坏数×(坏数+1)/2) / (坏数 × 好数)；不依赖 sklearn，可直接复算。
KS：    max(|累计坏样本占比 − 累计好样本占比|)，按风险分降序累计。

月度稳定性：逐月输出每个最终箱的样本量、成熟量、主指标风险率与相邻倒挂标记；
       汇总每个月的主指标倒挂次数与最大单次风险跌幅，识别异常月份。
```

- 分段验证按 Train / OOT × 自动通过 / 人工审核 / 拒绝六段输出规模与风险，风险应呈"自动通过 < 人工审核 < 拒绝"梯度；若 OOT 金额指标为空，应先检查表现成熟度，不能直接解释为零风险。

## 六、核心参数默认值（附录）

以下为本方案可直接修改的核心参数（报告 `06_附录 · 配置参数` 输出全量清单，便于版本管理）：

| 参数                                   | 默认值 | 含义                                                       |
| -------------------------------------- | ------ | ---------------------------------------------------------- |
| TRAIN_END_MONTH / OOT_START_MONTH      | 2025-10 / 2025-11 | Train 与 OOT 的分界月份                     |
| VALIDATION_MONTH_COUNT                 | 3      | Train 内末几个月作 Validation                             |
| INITIAL_BIN_COUNT                      | 20     | 等频初分箱数                                               |
| MIN / MAX / TARGET_FINAL_BIN_COUNT     | 6 / 8 / 7 | 最终档位数约束与目标档位数                           |
| HIGH_SCORE_HIGH_RISK                   | True   | 分数方向（高分高风险）                                     |
| MIN_MIDDLE / TAIL_BIN_SAMPLE_PCT       | 0.05 / 0.025 | 中间箱 / 头尾箱样本占比下限                       |
| MIN_FINAL_BIN_MATURE / BAD / GOOD      | 1000 / 20 / 200 | 普通箱成熟量 / 坏样本 / 好样本下限           |
| MIN_EXTREME_BIN_MATURE_COUNT           | 500    | 极端箱成熟量下限                                           |
| DEVELOPMENT_INVERSION_TOLERANCE        | 0.0    | Development 单调容差（严格非递减）                         |
| VALIDATION_INVERSION_TOLERANCE         | 0.003  | Validation 单调容差（允许 0.3pp 倒挂）                     |
| ADJACENT_PVALUE_TO_MERGE               | 0.10   | 候选生成阶段"差异不显著"的 p 值阈值                        |
| MIN_ADJACENT_ABS_RATE_DIFF             | 0.003  | 候选生成阶段"风险率接近"的差距阈值                         |
| PREFERRED / MAX_ACCEPTABLE_VALIDATION_PSI | 0.05 / 0.10 | 合箱选型的 PSI 偏好线与可接受线                  |
| PROTECT_LARGEST_RISK_JUMPS             | 1      | 保护的风险跳升最大边界数                                   |
| PROTECTED_BOUNDARY_PENALTY             | 100    | 跨越策略保护边界的代价                                     |
| PROTECT_EXTREME_INITIAL_BINS           | True   | 是否圈选并硬保护最好 / 最坏极端箱                          |
| EXTREME_BOUNDARY_PENALTY               | 10,000 | 极端边界跨越代价（放开硬保护时生效）                       |
| MERGE_COST_RATE_GAP_WEIGHT             | 100    | 合并代价中风险差距的权重                                   |
| MERGE_COST_IV_LOSS_WEIGHT              | 10     | 合并代价中 IV 损失的权重                                   |
| CANDIDATE_SCORE_WEIGHTS                | 见表 5 | 候选评分权重全量键值                                       |
| STRATEGY_CONFIG                        | 见表 6 | 自动通过 / 总接纳的累计与边际风险上限                       |
