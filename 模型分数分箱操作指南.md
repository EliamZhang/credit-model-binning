# 模型分数分箱操作指南

> **适用范围**：模型已经产出风险分或 PD 后，对模型分进行风险分层，并形成自动通过、人工审核、拒绝阈值。  
> **不适用范围**：本文不是建模阶段的单特征分箱指南。WOE、IV 仅作为模型分层的辅助指标。

---

## 一、最终执行方案

现行流程建议统一为：

> **统一数据口径 → 按时间划分策略调优集与 OOT → 等频 20 箱初分 → 检查样本量、坏样本量、单调性及相邻箱差异 → ChiMerge 合并为 6～10 个风险等级 → 在细粒度阈值上计算通过率、坏账率和边际风险 → 形成增长、平衡、保守三套策略 → 使用 OOT 确认 → 输出可上线的分箱和阈值配置。**

当前代码已完成等频分箱、相邻箱检验、ChiMerge、OOT、累计阈值曲线和三套方案框架；尚未完整接入 PD 校准、EL、收入、UE 和真实审核产能，因此当前策略方案只能视为**基于通过率和坏账率的候选方案**，不能直接视为利润最优方案。

### 三条基本原则

1. **分箱用于观察风险和收益曲线，阈值用于做业务决策，两者不能混为一谈。**阈值可以落在分箱边界上，也可以基于更细粒度分数逐点搜索。
2. 对模型分分层时，应优先观察**实际坏账率、校准后 PD、累计 EL 和收益曲线**；WOE、IV 更适合特征分箱，在模型分层中仅作为辅助参考。
3. 线上决策应使用预测时点可获得的预计收益；成熟样本回测应使用实际现金流，并反向校准收益模型。预测和回测服务于不同环节，并不冲突。

---

## 二、数据来源与字段

### 2.1 现行代码直接读取的数据

现行脚本读取两个中间文件，并通过 `application_id` 内连接：

| 输入文件 | 用途 | 必需字段 |
|---|---|---|
| `res/aus_old_risk_bid_mltmodel_v1_2_20260325_lgb_score.csv` | 模型评分结果 | `application_id`、`sample_datetime`、`aus_old_risk_bid_mltmodel_v1_2_v20260325_lgb_score` |
| `res/application_info.csv` | 贷后表现与金额信息 | `application_id`、`duedate_3m_30`、`duedate_1m_30`、`principal`、`estimate_principal_remaining_mob3`、`dpd_days_ever_mob3` |

当前核心配置：

```python
LABEL_COL = "duedate_3m_30"
CHIMERGE_LABEL_COLS = ["duedate_3m_30", "duedate_1m_30"]
SCORE_COL = "aus_old_risk_bid_mltmodel_v1_2_v20260325_lgb_score"
SCORE_HIGHER_IS_RISKIER = True
N_BINS = 20
OOT_CUT_DATE = "2025-10-21"
```

> **重要**：现行代码采用“分数越高，风险越高”。因此自动通过规则为 `score <= threshold`，拒绝规则为 `score > threshold`。更换模型时必须先确认分数方向。

现行脚本可按以下方式运行：

```bash
# 建议将脚本统一命名为 scr/binning.py
python scr/binning.py
```

运行前应确保两个输入 CSV 位于项目 `res/` 目录；当前默认输出为 `res/binning_result.md`。

### 2.2 建议统一到数据库的数据血缘

当前 Python 文件没有包含生成两个 CSV 的 SQL，因此上线前必须补齐数据血缘。结合当前项目字段，建议将贷后表现数据统一从 `ba.customer_profile_rawdata` 抽取；模型分来源表需替换为真实评分结果表。

推荐生成一张标准分箱样本宽表：

| 标准字段 | 来源 | 说明 |
|---|---|---|
| `application_id` | 模型评分表、`ba.customer_profile_rawdata` | 唯一关联键 |
| `sample_datetime` | 优先使用申请时点 `application_time` | 用于策略调优集和 OOT 时间切分 |
| `score` | 模型评分结果表 | 原始模型分，不应使用贷后信息计算 |
| `duedate_3m_30` | `ba.customer_profile_rawdata` | 主坏账标签，空值代表未成熟或未知 |
| `duedate_1m_30` | `ba.customer_profile_rawdata` | 辅助早期标签，用于稳定合并 |
| `principal` | `ba.customer_profile_rawdata` | 原始放款本金 |
| `estimate_principal_remaining_mob3` | `ba.customer_profile_rawdata` | MOB3 剩余本金 |
| `dpd_days_ever_mob3` | `ba.customer_profile_rawdata` | MOB3 内最大历史逾期天数 |
| `product/channel/month` | 业务申请表或宽表 | 用于分群和稳定性验证，建议补充 |
| `pd/lgd/ead/revenue/cost` | 风险及财务数据 | 用于后续 EL、收入和 UE 优化，当前未接入 |

建议 SQL 模板：

```sql
WITH score_data AS (
    SELECT
        application_id,
        score AS model_score
    FROM <MODEL_SCORE_TABLE>
    WHERE model_version = '<MODEL_VERSION>'
),
performance_data AS (
    SELECT
        application_id,
        application_time AS sample_datetime,
        duedate_3m_30,
        duedate_1m_30,
        principal,
        estimate_principal_remaining_mob3,
        dpd_days_ever_mob3
    FROM ba.customer_profile_rawdata
    WHERE application_time >= '<SAMPLE_START_DATE>'
)
SELECT
    s.application_id,
    p.sample_datetime,
    s.model_score,
    p.duedate_3m_30,
    p.duedate_1m_30,
    p.principal,
    p.estimate_principal_remaining_mob3,
    p.dpd_days_ever_mob3
FROM score_data s
INNER JOIN performance_data p
    ON s.application_id = p.application_id;
```

`<MODEL_SCORE_TABLE>`、模型版本和样本时间需在代码配置中填写，不能长期写死在脚本中。

### 2.3 数据质量检查

分箱前必须输出以下检查结果：

1. `application_id` 是否唯一，重复申请如何保留；
2. 评分表、表现表及关联后的样本数，左右表未匹配数量；
3. 分数缺失率、最小值、最大值、唯一值数量和同分占比；
4. 标签是否只包含 `0/1/NULL`；
5. 主标签成熟率，以及调优集和 OOT 的成熟率；
6. `sample_datetime` 是否真实代表申请或评分时点；
7. 本金、剩余本金和逾期字段的缺失及异常值；
8. 是否存在使用贷后信息生成模型分的数据泄漏。

主标签为 NULL 的样本不得进入坏账率计算。辅助标签缺失也不得直接填为 0，否则会把未知样本误判为好样本。


### 2.4 完整样本体系、成熟度与审批偏差

仅有“调优集 + OOT”可以运行现行代码，但标准方法论建议区分以下样本：

| 样本 | 主要用途 | 使用要求 |
|---|---|---|
| 训练集 | 训练模型 | 不直接用于选择策略阈值 |
| 校准集 | 将模型分映射为目标期限 PD | 样本充足时与策略调优集分开 |
| 策略调优集 | 设计分箱、合并等级、选择候选阈值 | 与训练集独立 |
| OOT 样本 | 在更晚时间段做最终确认 | 原则上只验证，不反复调参 |
| 线上成熟样本 | 回测真实坏账、现金流和 UE | 用于持续校准和策略迭代 |

样本量不足时，可以合并校准集和策略调优集，但应使用交叉验证、Bootstrap 或滚动时间验证评估不确定性。

#### 标签成熟度

优先使用完整表现窗口。若数据尚未成熟，只能使用 FPD7、FPD30 等早期指标时，需要：

1. 验证早期指标与最终损失之间的映射是否稳定；
2. 按产品、期限、渠道和月份重复验证；
3. 保存映射模型、参数版本、验证结果和更新时间；
4. 在样本成熟后用真实结果回溯修正。

#### 历史审批偏差

若历史样本仅包含已通过并已放款客户，直接用其评估新的拒绝阈值会受到审批选择偏差影响。优先使用：

- 全量申请人的外部风险表现；
- 随机审批或小流量探索样本；
- 历史政策变化形成的准实验样本；
- 必要时使用拒绝推断，但只作为敏感性分析，不能作为唯一依据。

### 2.5 风险标签与经济口径的前置统一

在分箱和阈值测算前，必须先统一以下定义，否则不同团队可能得到完全不同的结论。

#### 风险标签

至少明确：

- 违约定义：例如 FPD30、30+ DPD、90+ DPD、核销或其他公司统一口径；
- 表现窗口：放款后 30 天、90 天、6 个月或全生命周期；
- 统计单位：申请、客户、账户还是贷款合同；
- 是否包含提前结清、展期、重组、欺诈和重复申请；
- 未成熟样本如何排除或进行成熟度调整。

不同模型和产品只有在**违约定义与预测期限一致**时，PD 才具有直接可比性。

#### 经济口径

需要统一：

- PD、LGD、EAD 的定义和预测期限；
- 资金成本、获客成本、数据成本、运营成本、催收成本和资本成本；
- 利息、手续费、罚息、税费和回收金额的处理方式；
- 是评估合同期收益、年度收益，还是全生命周期收益；
- UE 是按申请、按获批账户、按放款账户，还是按单位本金计算。

---

## 三、参数配置

建议将参数从脚本顶部迁移到 YAML 或 JSON：

```yaml
model_name: aus_old_risk_bid_mltmodel_v1_2
model_version: v20260325
score_col: aus_old_risk_bid_mltmodel_v1_2_v20260325_lgb_score
score_higher_is_riskier: true
primary_label: duedate_3m_30
merge_labels:
  - duedate_3m_30
  - duedate_1m_30
sample_time_col: sample_datetime
oot_cut_date: 2025-10-21
initial_bins: 20
min_bins: 6
max_bins: 10
chi_p_threshold: 0.05
min_bin_size: 3000
min_bad_count: 100
```

参数含义：

| 参数 | 当前值 | 使用原则 |
|---|---:|---|
| 初始箱数 | 20 | 默认等频 20 箱；样本少或同分集中时可降为 10 箱 |
| 最终最少箱数 | 6 | 防止合并过度、失去风险区分 |
| 最终最多箱数 | 10 | 避免策略等级过碎 |
| 相邻箱显著性阈值 | 0.05 | `p >= 0.05` 表示差异不显著，可考虑合并 |
| 单箱最低样本量 | 3000 | 当前代码经验值，应按实际规模调整 |
| 单箱最低坏样本数 | 100 | 当前代码经验值，应结合坏账率及置信区间调整 |
| OOT 切分日期 | 2025-10-21 | 当前代码值，应按样本成熟度和业务时间重新确认 |

最低样本量和坏样本量不是固定行业标准，应结合总体坏账率、允许误差、产品和月份重新配置。

---

## 四、直接执行流程

### 步骤 1：构建标准样本

按 `application_id` 关联模型分与贷后表现，保留模型分、申请时点、主标签、辅助标签和金额字段。

关联后至少输出：

```text
评分表样本数
表现表样本数
成功关联样本数
评分表未匹配数
表现表未匹配数
重复 application_id 数
```

不要只使用 `inner join` 后的结果而不检查被丢弃的样本。

### 步骤 2：划分策略调优集与 OOT

现行代码按时间切分：

```python
tuning = data[data["sample_datetime"] < oot_cut]
oot = data[data["sample_datetime"] >= oot_cut]
```

要求：

- 调优集用于分箱、合并和候选阈值选择；
- OOT 只用于最终确认，不应反复查看后继续调参；
- 只保留主标签已成熟的样本计算风险表现；
- OOT 必须位于调优集之后；
- 若根据 OOT 修改方案，该 OOT 已变成调优样本，需要再保留更晚的独立样本验证。

### 步骤 3：等频 20 箱初分

在策略调优集上执行：

```python
bin_series, bin_edges = pd.qcut(
    tuning[score_col],
    q=20,
    duplicates="drop",
    retbins=True,
)
```

`duplicates="drop"` 用于处理大量同分造成的重复切点。实际箱数少于 20 时，应记录同分情况，不应强行制造切点。

首尾切点必须改为开口边界：

```python
bin_edges[0] = -np.inf
bin_edges[-1] = np.inf
```

这样 OOT 和线上样本即使超出调优集的分数范围，也不会被遗漏。

### 步骤 4：计算初始分箱指标

每箱至少输出：

| 指标 | 计算方式 | 用途 |
|---|---|---|
| 样本量 `n` | 该箱主标签有效样本数 | 判断统计充分性 |
| 坏样本数 `B` | `SUM(primary_label)` | 判断坏样本是否稀疏 |
| 坏账率 | `B / n` | 核心风险排序指标 |
| 标准误 `SE` | `sqrt(r(1-r)/n)` | 判断坏账率波动 |
| 累计通过率 | 低风险端累计样本 / 总样本 | 阈值分析 |
| 累计坏账率 | 低风险端累计坏样本 / 累计样本 | 阈值分析 |
| Lift | 箱坏账率 / 总体坏账率 | 风险浓度 |
| WOE、IV | 好坏分布差异 | 辅助参考 |
| AUC、KS | 全量分数排序能力 | 模型区分能力 |
| Spearman | 箱序与坏账率的秩相关 | 分箱单调性 |

WOE 出现零好样本或零坏样本时应使用统一平滑项，例如 0.5，不应直接将无穷值填成 0。


#### 关键统计公式

设初始分为 20 箱，第 `k` 个等频切点为：

\[
c_k=Q_{score}\left(\frac{k}{20}\right),\qquad k=1,2,\ldots,19
\]

设第 `i` 箱总样本数为 \(n_i\)，坏样本数为 \(B_i\)，则：

\[
r_i=\frac{B_i}{n_i}
\]

\[
SE(r_i)=\sqrt{\frac{r_i(1-r_i)}{n_i}}
\]

比较相邻两箱风险率时，可使用两比例差异近似检验：

\[
SE(r_1-r_2)=\sqrt{\frac{r_1(1-r_1)}{n_1}+\frac{r_2(1-r_2)}{n_2}}
\]

\[
z=\frac{r_1-r_2}{SE(r_1-r_2)}
\]

若差异较小、置信区间高度重叠，且不存在业务上的明显差异，可考虑合并。

#### ChiMerge 原理

ChiMerge 比较相邻两箱的好坏分布：

\[
\chi^2=\sum_{i=1}^{2}\sum_{j=1}^{2}\frac{(A_{ij}-E_{ij})^2}{E_{ij}}
\]

其中：

\[
E_{ij}=\frac{R_i\times C_j}{N}
\]

卡方值越小、`p` 值越大，说明两箱好坏分布越接近，越适合合并。期望频数过小时，应考虑精确检验、统一平滑或直接合并稀疏箱。

#### WOE 与 IV

对模型分层而言，WOE 和 IV 仅作为辅助，不应替代 AUC、KS、风险单调性、校准和经济价值。

设第 `i` 箱坏样本数为 \(B_i\)、好样本数为 \(G_i\)，全体坏样本数为 \(B\)、好样本数为 \(G\)，采用“坏 / 好”方向：

\[
WOE_i=\ln\left(\frac{B_i/B}{G_i/G}\right)
\]

\[
IV=\sum_i\left(\frac{B_i}{B}-\frac{G_i}{G}\right)\times WOE_i
\]

| IV 区间 | 常见解释 |
|---|---|
| `< 0.02` | 区分能力很弱 |
| `0.02～0.10` | 较弱 |
| `0.10～0.30` | 中等 |
| `0.30～0.50` | 较强 |
| `> 0.50` | 很强，需要检查稳定性、样本偏差和潜在泄漏 |

不同机构可能采用相反的 WOE 符号定义，代码、报告和历史结果必须保持一致。

### 步骤 5：检查并合并分箱

推荐按以下顺序处理：

1. 空箱、极小样本箱和坏样本稀疏箱；
2. 大量同分造成的边界异常；
3. 局部坏账率倒挂；
4. 相邻箱卡方或两比例 Z 检验差异不显著；
5. 调优集与 OOT 的分布和风险排序；
6. 最后才做有限的边界取整和业务适配。

现行 ChiMerge 规则：

- 初始 20 箱；
- 超过 `max_bins=10` 时，优先合并风险分布最接近的相邻箱；
- 箱数降至 10 以内后，仅在以下情形继续合并：
  - 所有指定标签的相邻箱差异均不显著；
  - 存在局部倒挂；
  - 任一箱样本量不足；
  - 任一箱主标签坏样本数不足；
- 最低保留 `min_bins=6` 个箱。

对于多个标签，建议每个标签分别计算相邻箱检验。不能将缺失辅助标签填为 0；应在该标签自己的有效样本范围内计算好坏分布。

#### 倒挂处理原则

坏账率倒挂不能一律强制合并：

- 若倒挂箱样本少、置信区间高度重叠或差异不显著，可视为抽样噪声并合并；
- 若倒挂差异显著，或只在特定产品、渠道、月份出现，应先排查数据、分群和模型失效，不应通过合并掩盖结构性问题。

#### 最终边界要求

最终策略阈值必须使用**真实切点**，不能使用每箱样本中观测到的 `score_max` 或 `score_min` 代替。否则线上出现训练样本中未观察到的分数时，可能产生区间空档。

同时统一区间语义，例如：

```text
(-∞, 0.10]
(0.10, 0.18]
(0.18, 0.25]
...
```

报告、JSON 配置和线上判断必须使用完全相同的左右开闭规则。

### 步骤 6：应用到 OOT

OOT 必须复用调优集确定的最终切点，禁止重新 `qcut`。

至少验证：

| 维度 | 指标 |
|---|---|
| 分布稳定性 | 各箱占比、PSI |
| 风险排序 | 各箱坏账率、Spearman、AUC、KS |
| 概率校准 | 平均 PD、实际坏账率、O/E、Brier Score；有 PD 时执行 |
| 策略表现 | 通过率、坏账率、金额损失、审核量 |
| 分群表现 | 月份、产品、渠道和关键客群 |

PSI 可参考：`<0.10` 相对稳定，`0.10～0.25` 需要观察，`>0.25` 需要重点排查。该区间只是经验预警，不是自动下线标准。

OOT 某箱样本为 0 时仍应保留该箱并按 0 占比计算 PSI，不能因为删除空箱而放弃 PSI。

### 步骤 7：生成累计阈值曲线

当前模型为高分高风险，因此每个候选阈值 `c` 的通过人群为：

```text
score <= c
```

候选阈值建议包含：

- 每个最终分箱边界；
- 分数等分位点；
- 业务系统允许配置的固定步长；
- 样本量允许时的每个唯一分数。

每个阈值至少输出：

| 指标 | 定义 |
|---|---|
| 累计通过率 | 阈值内样本数 / 全量样本数 |
| 累计坏账率（笔数） | 通过人群坏样本 / 通过人群主标签有效样本 |
| 边际坏账率 | 相比上一个阈值新增客群的坏账率 |
| 累计坏账率（金额） | 坏样本 MOB3 剩余本金 / 有效样本原始本金 |
| 预测 PD | 通过人群平均或金额加权 PD |
| EL | `SUM(PD × LGD × EAD)` |
| 风险后收入 | 预计收入－EL |
| 总 UE | 逐笔 UE 加总 |
| 边际 UE | 新增分数段带来的增量 UE |
| 审核量 | 进入人工审核区的申请数 |

金额坏账率建议定义为：

```text
SUM(estimate_principal_remaining_mob3 WHERE dpd_days_ever_mob3 >= 30)
/
SUM(principal)
```

但必须要求 `dpd_days_ever_mob3`、剩余本金和本金均非空且金额大于 0。当前代码中先将逾期字段比较为布尔值，会使 NULL 被当作未逾期，需修正。


#### 经济指标计算方法

当前代码尚未完整接入经济参数，但正式阈值优化应补充以下指标。

##### 1. 预期损失 EL

单笔预期损失：

\[
EL_j=PD_j\times LGD_j\times EAD_j
\]

| 参数 | 含义 | 常用估计方式 |
|---|---|---|
| PD | 目标期限内违约概率 | 校准后的模型 PD，或稳定分箱的实际坏账率 |
| LGD | 违约后的损失比例 | `1 - 折现后回收率`，并考虑催收费用 |
| EAD | 违约时风险暴露 | 违约时剩余本金及其他暴露 |

应明确采用 30 天、90 天、12 个月还是 Lifetime EL；不同期限的 EL 不能直接混用。

##### 2. 预计收入与风险后收入

等额本息产品的合同月供：

\[
A=P\times\frac{r(1+r)^n}{(1+r)^n-1}
\]

合同总利息：

\[
Interest_{contract}=A\times n-P
\]

但合同利息不等于预计可实现收入。预计收入还应考虑：

- 违约造成的未收利息；
- 提前还款造成的利息减少；
- 实际放款率和提款率；
- 手续费退还、减免和合规限制；
- 现金流发生时间和折现。

推荐使用预计现金流口径：

\[
风险后收入=预计可收利息及费用-预计本金损失
\]

若预计收入模型已经包含违约和提前还款影响，不得再次重复扣减相同损失。

##### 3. 单位经济价值 UE

\[
UE=预计收入-EL-资金成本-获客成本-数据成本-运营成本-催收成本-资本成本
\]

建议同时输出：

- 总 UE；
- 单笔 UE；
- 单位本金 UE；
- 边际新增客群 UE；
- UE 的置信区间或压力测试结果。

##### 4. 阈值优化约束

阈值不能只最大化单一指标，至少需要约束：

- 坏账率或 EL 不超过风险上限；
- 总 UE 不低于经营底线；
- 通过率、放款额或收入达到最低目标；
- 人工审核量不超过团队产能；
- 关键产品、渠道和客群满足合规与公平性要求；
- 压力情景下的损失仍可承受。

### 步骤 8：确定模型风险等级

最终风险等级通常保留 6～10 个，需同时满足：

- 风险总体单调，局部异常有解释；
- 单箱样本量和坏样本量充分；
- 相邻等级具有统计或业务差异；
- OOT 风险排序基本保持；
- 边界便于系统配置、解释和回滚；
- 不同产品、渠道和月份不存在明显失效。

风险等级只是稳定的风险刻画，不等于策略决策。策略可以在等级边界上设置，也可以在更细粒度分数上搜索后再对齐到可配置边界。

### 步骤 9：确定策略阈值

现行代码采用三段式策略：

```text
自动通过：score <= auto_threshold
人工审核：auto_threshold < score <= review_threshold
拒绝：score > review_threshold
```

当前三套方案是按最终箱数比例进行启发式切分：

| 方案 | 当前代码逻辑 | 说明 |
|---|---|---|
| 保守 | 最低风险约 1/3 自动通过，前约 2/3 可进入通过范围，其余拒绝 | 风险最低 |
| 平衡 | 最低风险约 1/2 自动通过，中间审核，仅拒绝最高风险箱 | 当前默认推荐 |
| 增长 | 除最高风险箱外自动通过，最高风险箱审核，不硬拒绝 | 通过率最高 |

该逻辑适合生成初始候选方案，但最终阈值应按业务约束优化：

```text
在坏账率或 EL 不超过上限、总 UE 不低于底线、审核量不超过产能的条件下，
选择总 UE 最大或位于风险—规模—收益 Pareto 前沿的阈值组合。
```

三套正式方案至少展示：

| 方案 | 自动通过阈值 | 审核区间 | 拒绝阈值 | 通过率 | 坏账率 | EL | 总 UE | 边际 UE | 审核量 | 主要风险 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 增长 |  |  |  |  |  |  |  |  |  |  |
| 平衡 |  |  |  |  |  |  |  |  |  |  |
| 保守 |  |  |  |  |  |  |  |  |  |  |

人工审核区不能只是机械选择中间分数段。只有补充信息可能改变决策，且审核后的增量收益大于审核成本时，才应进入人工审核。


#### 按业务目标选择阈值

| 业务目标 | 阈值选择方式 |
|---|---|
| 增长优先 | 在风险、资金和 UE 不突破底线的前提下，提高通过率 |
| 风险优先 | 在业务规模不低于底线的前提下，满足风险目标 |
| 利润优先 | 选择总 UE 或风险后总收益最大的方案，并检查边际 UE 是否为正 |
| 综合平衡 | 输出风险、规模、收益和审核量的 Pareto 前沿，由管理层选择风险偏好 |

正式方案除基准结果外，还应展示乐观和压力情景，包括 PD、LGD、资金成本、回收率和收入参数的变化。

#### 人工审核区的信息价值

人工审核不应简单等于中间一段分数。合理的审核对象通常同时满足：

1. 模型置信度或信息完整度较低；
2. 补充资料可能显著改变风险判断；
3. 审核后的预期增量收益高于审核成本；
4. 审核量不超过团队产能。

可用以下思路判断：

\[
人工审核净价值=审核后预期决策收益-直接自动决策收益-审核成本
\]

只有净价值为正的客群，才适合进入人工审核区。

### 步骤 10：输出和上线配置

建议输出以下文件：

```text
res/data_quality_report.md
res/initial_bin_stats.csv
res/adjacent_tests.csv
res/merge_history.csv
res/final_bin_stats.csv
res/final_bin_config.json
res/threshold_curve.csv
res/scheme_comparison.csv
res/binning_result.md
```

`final_bin_config.json` 建议结构：

```json
{
  "model_name": "aus_old_risk_bid_mltmodel_v1_2",
  "model_version": "v20260325",
  "score_higher_is_riskier": true,
  "label_definition": "duedate_3m_30",
  "sample_window": {
    "tuning_end": "2025-10-20",
    "oot_start": "2025-10-21"
  },
  "bin_edges": ["-Infinity", 0.10, 0.18, 0.25, "Infinity"],
  "interval_rule": "right_closed",
  "risk_grades": ["R1", "R2", "R3", "R4"],
  "strategy": {
    "auto_threshold": 0.18,
    "review_threshold": 0.25
  }
}
```

正式上线建议建立独立的**模型分层与策略阈值配置表**，至少包含：

| 字段类别 | 建议字段 |
|---|---|
| 模型信息 | 模型名称、模型版本、分数方向、PD 映射版本 |
| 适用范围 | 产品、渠道、客群、地区、申请类型和排除条件 |
| 分层配置 | 分箱边界、风险等级、箱内平均 PD 和历史坏账率 |
| 策略配置 | 自动通过阈值、审核区间、拒绝阈值及附加规则 |
| 口径信息 | 标签定义、样本窗口、表现窗口、EL 和 UE 口径 |
| 预期表现 | 通过率、坏账率、EL、收入、UE 和审核量 |
| 治理信息 | 生效日期、失效日期、审批人、变更原因、回滚版本 |

模型版本、PD 校准版本和策略版本应分开管理，以便判断变化来自模型、概率映射还是风险偏好。

---

## 五、效果验证与通过标准

正式上线前至少完成以下检查：

### 5.1 调优集

- 初始箱和最终箱风险曲线；
- AUC、KS、Spearman；
- 相邻箱卡方及 Z 检验；
- 样本量、坏样本量、金额暴露；
- 累计及边际坏账率；
- 有 PD 时检查校准；
- 有经济参数时检查 EL、风险后收入和 UE。

### 5.2 OOT

- 使用完全相同的分箱边界；
- 风险等级排序基本保持；
- AUC、KS 不出现无法解释的显著下降；
- PSI 和各箱占比变化可解释；
- 阈值下通过率、坏账率、EL 和审核量满足约束；
- 重要产品、渠道、月份和客群不存在明显失效。

### 5.3 上线后

持续监控：

| 类别 | 指标 |
|---|---|
| 数据 | 缺失率、异常率、模型覆盖率、评分失败率 |
| 分布 | 分数分布、风险等级占比、PSI/CSI |
| 排序 | AUC、KS、逐箱坏账率、尾部风险捕获率 |
| 校准 | 平均 PD、实际坏账率、O/E、校准截距和斜率 |
| 策略 | 通过率、审核率、拒绝率、人工调整率 |
| 经营 | 放款量、EL、实际损失、收入、风险后收入、UE |



### 5.4 PSI、校准和漂移处置

#### PSI

将分数按基准期固定分箱，设第 `i` 箱在基准期占比为 \(e_i\)，当前期占比为 \(a_i\)：

\[
PSI=\sum_i(a_i-e_i)\times\ln\frac{a_i}{e_i}
\]

| PSI 区间 | 常见判断 |
|---|---|
| `< 0.10` | 分布相对稳定 |
| `0.10～0.25` | 存在一定漂移，需要结合业务原因观察 |
| `> 0.25` | 漂移较明显，需要重点排查 |

上述范围仅是经验预警，不是监管硬标准。零占比箱应使用统一小值平滑，并记录处理方式。

#### O/E 校准

第 `i` 箱的 O/E 比：

\[
O/E_i=\frac{\sum_{j\in i}y_j}{\sum_{j\in i}PD_j}
=\frac{实际坏账率_i}{平均预测PD_i}
\]

- `O/E ≈ 1`：预测与实际较一致；
- `O/E > 1`：实际风险高于预测，模型低估风险；
- `O/E < 1`：实际风险低于预测，模型高估风险。

可将 `0.8～1.25` 作为初步预警范围，但必须结合样本量和置信区间判断。持续偏离时优先评估概率再校准，而不是直接移动分数阈值。

#### 漂移处置

| 问题表现 | 优先处置方式 |
|---|---|
| 分布漂移，但排序和校准稳定 | 观察客群结构，必要时调整经营预期 |
| 排序稳定，但整体 PD 偏高或偏低 | 概率再校准 |
| 局部客群失效 | 分群校准、增加策略规则或限制适用范围 |
| 风险偏好变化，模型仍稳定 | 调整策略阈值，不必重建模型 |
| 排序显著下降或数据关系改变 | 检查数据链路并考虑模型重建 |
| 收益或成本参数变化 | 更新经济参数后重新计算阈值 |


出现整体 PD 偏差但排序稳定时，优先重新校准；风险偏好变化但模型稳定时，仅调整阈值；排序明显下降或数据关系改变时，再考虑修复数据或重建模型。

---

## 六、后续 Vibe Coding 的重点优化

### P0：先保证数据与阈值正确

1. 将 CSV 数据来源改为配置化 SQL 或标准宽表，并记录数据版本；
2. 增加关联损失、重复键、标签成熟度和异常值检查；
3. 禁止将缺失辅助标签填成 0；
4. 修正金额坏账率中 NULL 逾期字段被当作好样本的问题；
5. 使用真实分箱边界生成阈值，不使用箱内观测 `score_max/min`；
6. 统一 `pd.cut` 的左右开闭规则、报告区间和线上判断；
7. 自动校验分数方向，防止高分高风险和高分低风险配置反转。

### P1：优化统计与策略算法

1. 使用 `sklearn.metrics.roc_auc_score` 和 `roc_curve` 计算 AUC/KS，正确处理同分；
2. PSI 固定使用完整分箱集合，即使 OOT 某箱为 0 也保留；
3. WOE 使用平滑项，不将无穷值直接填 0；
4. 对倒挂增加“是否显著”和“是否跨期复现”判断，不盲目合并；
5. 增加按月份、产品、渠道的分箱稳定性；
6. 将三套方案从按箱数比例切分升级为约束优化；
7. 接入 PD、LGD、EAD、收入、成本和 UE；
8. 将人工审核产能及审核增量价值纳入阈值选择。

### P2：提升工程可维护性

1. 去除函数对全局变量的依赖，参数显式传入；
2. 将数据加载、校验、分箱、合并、阈值和报告拆成独立模块；
3. 支持命令行参数和 YAML 配置；
4. 同时输出 CSV、JSON 和 Markdown；
5. 保存合并历史、人工调整原因和回滚版本；
6. 增加单元测试和小样本测试数据。

建议至少覆盖以下测试场景：

```text
高分高风险 / 高分低风险
大量相同分数
主标签或辅助标签缺失
某箱全好或全坏
OOT 分数超出调优集范围
OOT 某个箱没有样本
application_id 重复
金额字段缺失或为 0
箱数不足 3 个，无法设计三段策略
```

---

## 七、建议的代码结构

```text
binning_project/
├─ config/
│  └─ binning.yaml
├─ src/
│  ├─ data_loader.py
│  ├─ data_validator.py
│  ├─ initial_binning.py
│  ├─ bin_merger.py
│  ├─ metrics.py
│  ├─ threshold_optimizer.py
│  ├─ validator.py
│  └─ report_builder.py
├─ tests/
│  ├─ test_boundaries.py
│  ├─ test_missing_labels.py
│  ├─ test_score_direction.py
│  └─ test_oot_psi.py
├─ res/
└─ run_binning.py
```

主程序仅负责串联：

```python
def main(config_path: str) -> None:
    config = load_config(config_path)
    data = load_data(config)
    validate_data(data, config)

    tuning, oot = split_samples(data, config)
    initial_bins = build_quantile_bins(tuning, config)
    final_bins, merge_history = merge_bins(tuning, initial_bins, config)

    tuning_result = evaluate_bins(tuning, final_bins, config)
    oot_result = evaluate_bins(oot, final_bins, config)

    threshold_curve = build_threshold_curve(tuning, final_bins, config)
    schemes = optimize_schemes(threshold_curve, config)

    export_results(
        config=config,
        final_bins=final_bins,
        merge_history=merge_history,
        tuning_result=tuning_result,
        oot_result=oot_result,
        threshold_curve=threshold_curve,
        schemes=schemes,
    )
```


---

## 附录一：主流模型分箱方法

| 方法 | 核心思想 | 优点 | 主要局限 | 适用场景 |
|---|---|---|---|---|
| 等距分箱 | 每个分数区间宽度相同 | 边界简单、易解释 | 分布偏斜时部分箱样本过少 | 分数分布较均匀、固定评分卡 |
| 等频分箱 | 每箱样本量大致相同 | 风险率估计相对稳定 | 边界不整齐，同分可能减少箱数 | 最常用的初始观察方法 |
| 固定业务分段 | 按整分、历史评级或既有政策切分 | 易管理、版本兼容 | 未必适应新模型分布 | 已有成熟策略体系 |
| 聚类分箱 | 按分数密度或聚类结果划分 | 适应非均匀分布 | 边界和解释可能不稳定 | 探索性分析 |
| 单调合并 | 合并相邻箱，使风险率总体单调 | 排序清晰、易解释 | 可能牺牲局部区分度 | 信贷风险分层 |
| ChiMerge | 按相邻箱好坏分布的卡方差异合并 | 统计依据明确、易自动化 | 对样本量和参数敏感 | 有明确好坏标签 |
| 决策树 / 熵 / MDL | 选择最能区分目标的切点 | 可识别非线性边界 | 易过拟合、边界可能碎片化 | 风险分布非线性明显 |
| 约束最优分箱 | 在单调性、箱数、样本量等约束下优化目标 | 可复现、约束完整 | 实施复杂，需防止样本内优化 | 标准化建模平台 |
| PD 校准分层 | 先校准为 PD，再按风险区间分层 | 便于跨模型和跨产品比较 | 依赖校准质量 | 多模型统一评级管理 |
| 收益 / 成本分层 | 按 EL、收益、成本或 UE 确定边界 | 与经营目标直接一致 | 依赖经济参数准确性 | 利润导向策略 |
| 混合人工方案 | 自动初分和合并后做有限业务调整 | 符合系统和经营实际 | 需严格治理，避免随意改动 | 行业常见最终方案 |

实践中通常组合使用：

> **等频初分 → 单调与统计合并 → 跨期稳定性验证 → 有限业务调整 → 逐阈值收益优化 → OOT 确认。**

---

## 附录二：常见误区

1. **直接把 20 个初始箱当成 20 个策略等级**：分析颗粒度不等于最终策略颗粒度。
2. **只看坏账率，不看收益和成本**：低风险不一定代表总利润最高。
3. **只看单笔 UE，不看总 UE**：可能错过具有正边际价值的规模增长。
4. **使用训练集选择阈值**：容易高估模型与策略效果。
5. **反复使用同一份 OOT 调参**：会失去真正的独立验证。
6. **对各箱坏账率直接平均**：忽略样本量和金额暴露差异。
7. **预计收入和违约损失重复扣减**：会造成收益低估。
8. **机械使用 PSI、IV 或固定阈值**：经验规则不能替代统计、业务和经济判断。
9. **仅在历史已通过客户上评估新阈值**：会受到审批选择偏差影响。
10. **模型版本、PD 校准版本和策略版本使用同一个版本号**：难以判断变更来源，也不利于回滚。
11. **用箱内观测最大值或最小值代替真实切点**：线上可能出现边界空档。
12. **将缺失标签填成 0**：会把未成熟或未知样本错误当成好样本。

---

## 八、执行完成后的最终交付物

每次模型分箱应固定交付：

1. **数据口径表**：来源表、字段、标签、时间和样本量；
2. **最终风险等级表**：真实切点、区间规则、样本量、坏账率、Lift、PD 和 OOT 表现；
3. **累计阈值表**：通过率、累计及边际风险、EL、收入、UE 和审核量；
4. **三套策略方案**：增长、平衡、保守及其约束和压力结果；
5. **上线配置**：模型版本、校准版本、策略版本、生效时间和回滚版本；
6. **验证报告**：调优集、OOT、分群稳定性和已知限制。

最终上线原则：

> **分箱用于建立稳定、可解释的风险等级；阈值用于在风险、规模、收益和审核产能之间做决策。先保证数据口径和边界正确，再优化统计合并，最后接入经济价值进行阈值选择。**
